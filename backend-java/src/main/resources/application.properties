# OceanShield OPS — Spring Boot 3 Backend Configuration
server.port=8080
spring.application.name=ocean-shield-ops

# Jackson formatting
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.default-property-inclusion=non_null

# Logging
logging.level.root=INFO
logging.level.com.oceanshield.ops=DEBUG
